@page "/{*route}"

@inject NavigationManager Navigation

@code {
    [Parameter] public string ? Route { get; set; }

    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"/error/404?route={Uri.EscapeDataString(Route ?? "")}", forceLoad: false);
    }
}